/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Portafolios_Coders',
  assetPrefix: '/Portafolios_Coders/',
  images: {
    unoptimized: true
  }
}

module.exports = nextConfig
